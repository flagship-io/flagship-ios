name: Swift

on:
  push:
  pull_request:
    branches: [ master ]

jobs:
  lint:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - name: checkout
    
    - name: Lint
    - run: swiftlint
      

     
    
